#include <stdio.h>
int main() {
	int c,a,b;
	 scanf("%d %d %d", &c,&a,&b);
	 
	 c = (2*a)+b;
	 printf("1st equation %d\n",c);
	 
	 c= (a*a*a*a*a)+(a*a*a*a)+ (2*(a*a*a)) + (3*(a*a*a))+3;
	 printf("second equation %d\n",c);
	 
	 c = 2+(3*a);
	  printf("third equation %d\n",c);
	 
	 
}
